<android.support.design.widget.CoordinatorLayout
    android:id="@+id/main_content"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- TODO: Update blank fragment layout -->
    <android.support.v7.widget.RecyclerView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/tournament_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/tournament_list_paddingButton"
        android:paddingLeft="@dimen/tournament_list_paddingLeft"
        android:paddingRight="@dimen/tournament_list_paddingRight"
        android:paddingTop="@dimen/tournament_list_paddingTop"
        android:scrollbars="vertical" />

    <android.support.design.widget.FloatingActionButton
        android:src="@android:drawable/ic_input_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="normal"
        app:layout_anchor="@id/tournament_list"
        app:layout_anchorGravity="bottom|right|end"
        android:id="@+id/tour_add"
        android:layout_margin="@dimen/tour_add_margin"/>
</android.support.design.widget.CoordinatorLayout>
